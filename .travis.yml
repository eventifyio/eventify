language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
# command to install dependencies
install: "pip install -r requirements.txt"
# fix perms
before_install:
  - chmod +x test/test_eventify.py
# command to run tests
script: pytest test/test_eventify.py
after_success:
  - coveralls
